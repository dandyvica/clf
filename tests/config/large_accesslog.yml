---
global:
  path: foo

searches:
  - logfile: tests/logfiles/large_access.log
    tags: 
      - name: tag1
        options: "dontbreak"
        script:
          path: tests/scripts/notify.py
          args: ['arg1']        
        patterns:
          critical:
            regexes: ['^195\.(?P<ip>[\d\.]+)'] 
