---
global:
  path: "//foo"
  snapshotdir: /tmp

searches:
  - logfile: /var/log/kern.log
    tags:
      - name: tag1
        script:
          path: /tmp/my_script.sh,
          args: ['arg1', 'arg2', 'arg3']
        patterns:
          critical:
            regexes: ["associated"]