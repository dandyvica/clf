---
global:
  path: "//foo"
  snapshotdir: /tmp

searches:
  - loglist: { cmd: "find", args: ["/tmp", "-type", "f"] }
    tags:
      - name: tag1
        script:
          path: /tmp/my_script.sh,
          args: ['arg1', 'arg2', 'arg3']
        patterns:
          critical:
            regexes: ["associated"]